{
  "name": "mcp-servers",
  "version": "1.0.0",
  "description": "Community MCP server definitions for McpMux",
  "private": true,
  "scripts": {
    "validate": "node scripts/validate.js",
    "validate:all": "node scripts/validate.js --all",
    "build": "node scripts/build-bundle.js",
    "check-conflicts": "node scripts/validate.js --check-conflicts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "ajv": "^8.17.0",
    "ajv-formats": "^3.0.0",
    "glob": "^11.0.0",
    "vitest": "^4.0.18"
  }
}
